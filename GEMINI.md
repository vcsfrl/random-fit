# Gemini Instructions

Use `AGENTS.md` at the repository root as the canonical AI context for this repository.

Do not duplicate project guidance here; keep this file as a thin entrypoint that points back to `AGENTS.md`.
