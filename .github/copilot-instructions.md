# GitHub Copilot Instructions

Use `AGENTS.md` at the repository root as the canonical project context for this repository.

It contains the project summary, architecture, conventions, testing patterns, file locations, build commands, and implementation details that should guide code changes and reviews.
